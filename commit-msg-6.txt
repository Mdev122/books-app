feat: dynamic rating bar color and width (Ćwiczenie 6)

- szablon #template-book: book__rating__fill ma teraz atrybut
  style="width: {{ ratingWidth }}%; background: {{ ratingBgc }}"
  zamiast statycznego, szarego paska
- determineRatingBgc(rating) - zwraca odpowiedni gradient CSS
  (string) zależnie od progu:
    rating < 6        -> żółty gradient (#fefcea -> #f1da36)
    rating <= 8        -> zielony (#b4df5b)
    rating <= 9        -> ciemnozielony (#299a0b)
    rating > 9         -> różowy (#ff0084)
- render(): przed wygenerowaniem HTML dla każdej książki liczy
  ratingBgc (z determineRatingBgc) oraz ratingWidth (rating * 10),
  po czym przekazuje do szablonu rozszerzoną kopię obiektu książki
  (Object.assign({}, book, {ratingBgc, ratingWidth})) zamiast
  modyfikować DOM po wyrenderowaniu
